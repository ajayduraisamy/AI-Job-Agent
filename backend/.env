
DB_PATH=ai_jobs.db

# Gmail IMAP (use app password if 2FA enabled)
GMAIL_IMAP=imap.gmail.com
GMAIL_EMAIL=youremail@gmail.com
GMAIL_PASSWORD=your_imap_or_app_password

# Resume path to upload when applying
RESUME_PATH= Full_Stack_Developer_Ajay.pdf

# Country/location for searches
JOB_LOCATION=India
JOB_ROLE=software engineer

# Selenium (if using local chromedriver)
SELENIUM_DRIVER_PATH=/path/to/chromedriver

# Mode: "auto" will attempt to apply automatically; "manual" will queue for manual approval
APPLY_MODE=manual

# Local LLM adapter config (if needed)
LOCAL_LLM_ENDPOINT=http://localhost:8000/generate   # or leave blank and use internal stub
